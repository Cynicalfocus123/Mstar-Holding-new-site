{
  "name": "mstar-holding-new-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:github": "vite build --base=/Mstar-Holding-new-site/",
    "build:hostinger": "vite build --base=/",
    "preview": "vite preview --host 127.0.0.1",
    "lint": "prettier --check .",
    "test": "npm run lint",
    "weight:audit": "node scripts/asset-audit.mjs"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "vite": "^8.0.16"
  }
}
